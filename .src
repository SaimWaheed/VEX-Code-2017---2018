#pragma config(Motor,  port1,           leftDirect,    tmotorVex393HighSpeed_HBridge, openLoop)
#pragma config(Motor,  port2,           leftChain,     tmotorVex393HighSpeed_MC29, openLoop, reversed)
#pragma config(Motor,  port3,           rightDirect,   tmotorVex393HighSpeed_MC29, openLoop)
#pragma config(Motor,  port4,           rightChain,    tmotorVex393HighSpeed_MC29, openLoop, reversed)
//*!!Code automatically generated by 'ROBOTC' configuration wizard               !!*//

task main()
{
 while(true){
   if(abs(vexRT[Ch3]) > 20){
    	motor[leftDirect] = motor[leftChain] = vexRT[Ch3];
    }
    	else{
    		motor[leftDirect] = motor[leftChain] = vexRT[Ch3];
    	}
	if(abs(vexRT[Ch2]) > 20){
    	motor[rightDirect] = motor[rightChain] = vexRT[Ch2];
  }
    else{
    	motor[rightDirect] = motor[rightChain] = vexRT[Ch2];
    }
}
}
